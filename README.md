# AutoDBA-Proxy
I am building an AI-powered middleware proxy that sits between a backend server and a MySQL database. It intercepts traffic, uses Machine Learning to learn the query patterns, and autonomously optimizes the database data structures in real-time.
